[package]
name = "hashcalc-ffi"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "hashcalc_ffi"
crate-type = ["staticlib"]

[dependencies]
hashcalc-core = { path = "../core" }
strum = { version = "0.28.0", features = ["derive"] }
